---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Signup Form Integration

## Objective
Connect the registration form in `signup.html` to the backend `/api/register` endpoint and handle user feedback.

## Context
- .gsd/SPEC.md
- frontend/js/signup.js
- backend/main.py

## Tasks

<task type="auto">
  <name>Update Signup JS Logic</name>
  <files>frontend/js/signup.js</files>
  <action>
    Modify the submit event listener in `frontend/js/signup.js`:
    - Replace the `setTimeout` simulation with a real `fetch` call to `http://localhost:8000/api/register`.
    - Send `fullname`, `email`, and `password` as JSON.
    - Handle success: Alert user and redirect to `login.html`.
    - Handle error: Show error message (e.g., "Email already registered") using the existing `showError` function.
  </action>
  <verify>Manually test signup with a new email and check if it appears in the database.</verify>
  <done>User can create an account and see real error messages from the backend.</done>
</task>

## Success Criteria
- [ ] Account creation via frontend works against the real SQLite backend.
- [ ] Duplicate email error is correctly displayed to the user.
