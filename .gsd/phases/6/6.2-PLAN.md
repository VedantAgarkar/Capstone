---
phase: 6
plan: 2
wave: 1
---

# Plan 6.2: Theme Login & Signup Pages

## Objective
Update the `login.html`, `signup.html`, and their styling to exactly match the global navy blue, gold, and white accent theme. Currently, these pages clash with the rest of the website's dark navy/gold aesthetic.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- frontend/login.html
- frontend/signup.html
- frontend/css/login.css
- frontend/css/global.css

## Tasks

<task type="auto">
  <name>Align Login and Signup Styles to Theme</name>
  <files>
    - frontend/css/login.css
  </files>
  <action>
    Modify `login.css` (which is shared by login and signup) to implement the navy blue (`#1a1f3a`), gold (`#B79347`), and white palette. Ensure form inputs, buttons, and card backgrounds flow naturally into the theme. Replace bright colors or disjointed accents. Make the inputs have a premium glassmorphic or sleek minimalist look matching the site. Consider using `global.css` variables if feasible, or strictly code those hex values.
  </action>
  <verify>
    Ensure buttons have hover effects with the gold variant (`#a6854a`) or identical to the global `.login-btn`.
  </verify>
  <done>
    `login.css` relies entirely on the new thematic colors and eliminates the old, mismatching aesthetic.
  </done>
</task>

<task type="auto">
  <name>Refine Authentication Layouts</name>
  <files>
    - frontend/login.html
    - frontend/signup.html
  </files>
  <action>
    Update any inline properties left over inside `login.html` and `signup.html`. Ensure logos or headers correctly display `nav` elements matching `primary-dark`. If needed, re-write text colors to ensure clear contrast against the navy blue or light backgrounds. Make sure `global.css` is linked right after the title tag to inherit standard fonts and base styles.
  </action>
  <verify>
    Ensure the Javascript API calls logic (`js/login.js`, `js/signup.js`) is untouched during layout editing.
  </verify>
  <done>
    Login and Signup pages look cohesive, with high-contrast text and a premium appearance matching the application's overall design system.
  </done>
</task>

## Success Criteria
- [ ] Login and Signup pages blend with the naval blue and gold layout completely.
- [ ] No regression or visual bugs exist in the input forms.
