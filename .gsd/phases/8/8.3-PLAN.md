# Plan 8.3: Localize Medical AI Bot & Final Polish

## Objective
Enable multi-language support for the Medical AI Bot and perform a final audit of the localization across all health apps.

## Context
- `backend/routes/bot.py`
- `backend/routes/diabetes.py`: (Audit)

## Tasks

<task type="auto">
  <name>Localize Medical AI Bot</name>
  <files>
    - backend/routes/bot.py
  </files>
  <action>
    - Implement language detection from query parameters.
    - Localize the initial greeting message.
    - Localize the `st.chat_input` placeholder and footer.
    - Update the `system_prompt` to instruct the AI to respond in the user's selected language.
  </action>
  <verify>Run bot with `?lang=mr` and check if instructions and greetings are in Marathi.</verify>
  <done>The Bot is bi-lingual.</done>
</task>

<task type="auto">
  <name>Final Localization Audit</name>
  <files>
    - backend/routes/diabetes.py
    - backend/utils.py
  </files>
  <action>
    - Audit `diabetes.py` to ensure no hardcoded English strings remain.
    - Check the shared footer in `utils.py` (if applicable) or in each route file.
    - Ensure PDF report titles and patient info labels are localized in the final output.
  </action>
  <verify>Quick scan for hardcoded strings in all 4 apps.</verify>
  <done>All apps consistently use the localization system.</done>
</task>

## Success Criteria
- [ ] User can switch from English to Marathi on the homepage and have a seamless 100% Marathi experience throughout all assessments and the chatbot.
