# Plan 4.1 Summary: Schema Update & Admin Creation

## Objective
Update the SQLite database to support administrative roles and data tracking for prediction history.

## Changes
- Modified `backend/database.py` to add `is_admin` column to `users` table.
- Created `predictions` table for logging prediction history.
- Created `scripts/create_admin.py` and promoted `admin@test.com` to admin status.

## Files Touched
- `backend/database.py`
- `scripts/create_admin.py` (New)

## Verification
- Database initialization ran successfully.
- Admin creation script confirmed user promotion.
