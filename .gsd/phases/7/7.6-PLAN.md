# Plan 7.6: Fix Model Warnings and Feature Mismatches

## Objective
Silence the version mismatch warnings and fix the "X does not have valid feature names" warning in the Streamlit logs by passing DataFrames instead of raw numpy arrays to the scalers and models.

## Context
- `backend/routes/heart.py`
- `backend/routes/diabetes.py`
- `backend/routes/parkinsons.py`

## Tasks

<task type="auto">
  <name>Fix Heart Disease Model Warnings</name>
  <files>
    - backend/routes/heart.py
  </files>
  <action>
    - Add `import warnings` and `warnings.filterwarnings("ignore", category=UserWarning)` and `warnings.filterwarnings("ignore", message=".*unpickle estimator.*")`.
    - Convert `features` to a `pd.DataFrame` with column names: `['age', 'sex', 'cp', 'trestbps', 'chol', 'fbs', 'restecg', 'thalach', 'exang', 'oldpeak', 'slope', 'ca', 'thal']`.
  </action>
  <verify>Check heart.py for DataFrame conversion and warning suppression.</verify>
  <done>Heart Disease route runs without visible feature name or version warnings.</done>
</task>

<task type="auto">
  <name>Fix Diabetes Model Warnings</name>
  <files>
    - backend/routes/diabetes.py
  </files>
  <action>
    - Add `import pandas as pd`, `import warnings`.
    - Add warning filters.
    - Convert `features` to a `pd.DataFrame` with column names: `['Pregnancies', 'Glucose', 'BloodPressure', 'SkinThickness', 'Insulin', 'BMI', 'DiabetesPedigreeFunction', 'Age']`.
  </action>
  <verify>Check diabetes.py for DataFrame conversion and warning suppression.</verify>
  <done>Diabetes route runs without visible warnings.</done>
</task>

<task type="auto">
  <name>Fix Parkinson's Model Warnings</name>
  <files>
    - backend/routes/parkinsons.py
  </files>
  <action>
    - Add `import pandas as pd`, `import warnings`.
    - Add warning filters.
    - Convert `features` to a `pd.DataFrame` with column names: `['MDVP:Fo(Hz)', 'MDVP:Fhi(Hz)', 'MDVP:Flo(Hz)', 'MDVP:Jitter(%)', 'MDVP:Jitter(Abs)', 'MDVP:RAP', 'MDVP:PPQ', 'Jitter:DDP', 'MDVP:Shimmer', 'MDVP:Shimmer(dB)', 'Shimmer:APQ3', 'Shimmer:APQ5', 'MDVP:APQ', 'Shimmer:DDA', 'NHR', 'HNR', 'RPDE', 'DFA', 'spread1', 'spread2', 'D2', 'PPE']`.
  </action>
  <verify>Check parkinsons.py for DataFrame conversion and warning suppression.</verify>
  <done>Parkinson's route runs without visible warnings.</done>
</task>

## Success Criteria
- [ ] No feature name warnings in console logs.
- [ ] No scikit-learn version mismatch warnings in console logs.
- [ ] Predictions still work correctly across all apps.
